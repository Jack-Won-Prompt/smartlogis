# DESIGN.md — SmartLogis 디자인 시스템 "Control Tower"

이 문서는 SmartLogis의 모든 화면이 따라야 하는 디자인 사양이다.
CLAUDE.md와 함께 프로젝트 루트에 두고, UI 코드를 작성하는 모든 세션에서 이 토큰과 규칙을 그대로 사용한다.
**여기에 정의되지 않은 색/폰트/그림자/애니메이션을 임의로 추가하지 않는다.**

---

## 0. 디자인 컨셉

**"의료 물류 관제탑(Control Tower)"** — 수술실에 들어가는 제품을 다루는 시스템이다.
디자인의 목표는 "예쁘다"가 아니라 **"한눈에 상태가 읽히고, 실수할 수 없게 만드는 정밀함"**이며,
그 정밀함 자체가 세련됨으로 보이게 한다. 기준 이미지는 의료기기 계측 장비와 항공 관제 UI:
차분한 저채도 표면 위에 데이터만 또렷하게 떠 있고, 색은 오직 **의미(상태)** 를 전달할 때만 쓴다.

세 가지 금지 사항 (흔한 관리자 템플릿의 냄새를 제거):
1. 무의미한 카드 그림자 남발 금지 — 그림자는 오버레이(모달/드롭다운)에만.
2. 알록달록한 차트 기본 팔레트 금지 — 차트 색은 아래 데이터 팔레트만.
3. 보라-남색 그라데이션, 유리모피즘, 과한 라운드 금지.

---

## 1. 컬러 토큰

라이트 모드 기본 + 다크 모드 지원(창고 야간 작업 대응). CSS 변수로 정의하고 Tailwind에 매핑한다.

### 1.1 표면 / 잉크 (Neutral — 청회색 계열, 순수 회색 금지)
```css
:root {
  --surface-0: #F6F8FA;  /* 앱 배경 */
  --surface-1: #FFFFFF;  /* 카드, 테이블 */
  --surface-2: #EEF2F5;  /* 필터 영역, 테이블 헤더, hover */
  --line:      #DDE4EA;  /* 구분선 (기본 보더는 이것 하나만) */
  --line-strong: #B9C4CE;

  --ink-900: #101B26;    /* 제목, 주요 데이터 */
  --ink-700: #33424F;    /* 본문 */
  --ink-500: #5E7080;    /* 보조 텍스트, 라벨 */
  --ink-300: #94A5B3;    /* placeholder, 비활성 */
}
.dark {
  --surface-0: #0D1620;
  --surface-1: #142130;
  --surface-2: #1B2C3E;
  --line:      #24384C;
  --line-strong: #3A5570;
  --ink-900: #EDF2F6;
  --ink-700: #C3D0DB;
  --ink-500: #8AA0B2;
  --ink-300: #5B7284;
}
```

### 1.2 브랜드 (Primary — 딥 틸)
의료의 청결함 + 물류의 신뢰를 담은 **딥 틸(deep teal)**. 인디고/파랑 SaaS 기본값을 쓰지 않는다.
```css
--brand-700: #0B5E5C;   /* hover, 강조 텍스트 */
--brand-600: #0E7473;   /* 기본 버튼, 활성 상태, 링크 */
--brand-500: #159694;   /* 차트 주 색상 */
--brand-100: #DCF0EF;   /* 선택 배경, 활성 탭 배경 */
--brand-050: #EEF8F7;
```

### 1.3 시맨틱 (상태 색 — 이 시스템의 심장)
상태 색은 장식이 아니라 **업무 신호등**이다. 아래 매핑을 시스템 전체에서 절대 바꾸지 않는다.
```css
--ok-600:   #1E8A5B;  --ok-100:   #DEF3E8;   /* 정상, 승인, 재고 충분 */
--warn-600: #B4700A;  --warn-100: #FBEFD8;   /* 주의: 안전재고 근접, D-60 */
--crit-600: #C2362B;  --crit-100: #FBE4E1;   /* 위험: 재고 미달, D-30, 반려, 유통기한 경과 */
--info-600: #2563A8;  --info-100: #E0ECF9;   /* 진행중, 전송됨, 배송중 */
--hold-600: #6B7A88;  --hold-100: #EDF1F4;   /* 임시저장, 취소, 비활성 */
```

### 1.4 차트 데이터 팔레트 (Chart.js 전용, 순서 고정)
```
#159694  틸(주)   #2563A8  블루   #7A5AC8  퍼플
#B4700A  앰버     #C2362B  레드   #5E7080  그레이
```
- 단일 시리즈 차트는 무조건 틸 단색.
- 상태를 표현하는 차트(재고 수준 등)는 데이터 팔레트가 아닌 **시맨틱 색**을 쓴다.

---

## 2. 타이포그래피

숫자와 코드가 주인공인 시스템이다. **본문용 폰트와 데이터용 모노 폰트를 엄격히 분리**한다.

| 역할 | 폰트 | 용도 |
|---|---|---|
| Display/Heading | **SUIT Variable** (700/800) | 페이지 타이틀, KPI 숫자 라벨, 빈 화면 메시지 |
| Body | **Pretendard Variable** (400/500/600) | 본문, 테이블 텍스트, 버튼, 폼 |
| Data Mono | **IBM Plex Mono** (400/500) | Lot 번호, 제품코드, GTIN, 수량, 금액, 날짜, 바코드 원문 |

```css
font-family:
  body:    "Pretendard Variable", Pretendard, sans-serif;
  display: "SUIT Variable", "Pretendard Variable", sans-serif;
  mono:    "IBM Plex Mono", monospace;
```

**핵심 규칙:**
- 수량·금액·코드·Lot·날짜는 **반드시 `font-mono` + `font-variant-numeric: tabular-nums`** — 테이블에서 자릿수가 세로로 정렬되어야 한다.
- 금액은 우측 정렬, 수량은 우측 정렬, 텍스트는 좌측 정렬. 예외 없음.
- 타입 스케일: `28/22/17/15/13.5/12px`. 테이블 본문은 13.5px, 테이블 헤더는 12px 대문자 자간 +0.04em (`--ink-500`).
- KPI 숫자는 SUIT 800, 32~40px, `tabular-nums`.

---

## 3. 레이아웃 / 공간

- **좌측 고정 사이드바 232px** (다크 네이비 `#101B26` 고정 — 라이트/다크 모두 동일. 관제탑의 "기둥").
  메뉴 그룹: 대시보드 / 기준정보 / 재고 / 입출고 / 사용분 / 정산 / 공급사 / 관리.
  활성 메뉴: 좌측 3px 틸 바 + `--brand-100/10%` 배경.
- 상단바 56px: 좌측 breadcrumb, 우측 [전역 스캔 입력창] [알림 벨+뱃지] [조직 배지] [프로필].
- 콘텐츠 최대폭 제한 없음(테이블 시스템), 콘텐츠 패딩 24px, 카드 내부 패딩 20px.
- 간격 스케일: 4/8/12/16/20/24/32. 이외 값 금지.
- 라운드: 입력·버튼 **6px**, 카드 **10px**, 배지/칩 **풀 라운드**. 이 셋뿐.
- 보더: 카드와 테이블은 `1px solid var(--line)`. 그림자 대신 보더로 구획한다.
- 그림자는 두 개만: `--shadow-overlay`(드롭다운/팝오버), `--shadow-modal`(모달). 카드에는 없음.

---

## 4. 시그니처 요소 (SmartLogis만의 정체성 — 반드시 구현)

### 4.1 Flow Rail — 재고 흐름 레일
`공급사 ▸ 물류창고 ▸ 거점병원 ▸ 사용/정산` 4노드 흐름을 시스템 전역 모티프로 쓴다.
- 입고/출고/사용분 **상세 화면 상단**에 가로 레일로 표시: 현재 문서가 흐름의 어느 단계인지
  해당 노드가 틸로 점등되고, 지나온 구간은 실선, 남은 구간은 점선.
- 대시보드 상단에도 미니 Flow Rail: 각 구간의 오늘 이동 수량이 노드 사이에 mono 숫자로 표시.
- 구현: 단일 SVG 컴포넌트 `<FlowRail current="WAREHOUSE" />`. 이 시스템을 열면 가장 먼저 기억되는 요소.

### 4.2 Lot Chip — 데이터 칩
Lot/유통기한/코드는 일반 텍스트로 흘리지 않고 **칩**으로 감싼다.
```
[ LOT A23K01 ]  [ EXP 2027-03 ]  [ GTIN 08806…41 ]
```
- `font-mono 12px`, `--surface-2` 배경, 1px `--line` 보더, 풀 라운드, 라벨(LOT/EXP)은 `--ink-300`.
- **유통기한 칩은 상태에 따라 자동 변색**: D-90 이내 warn, D-30 이내/경과 crit. 색만으로도 위험이 읽힌다.
- 클릭 시 해당 Lot 추적 화면으로 이동(전 화면 공통).

### 4.3 Scan Pulse — 스캔 피드백
바코드 스캔 성공 시: 입력창 테두리가 틸로 1회 펄스(300ms) + 파싱된 Lot Chip 3개가
40ms 간격으로 순차 등장(stagger). 실패 시: crit 색 셰이크 1회(200ms) + 인라인 사유.
스캔이 "기분 좋게 꽂히는" 감각이 이 시스템의 UX 품질을 결정한다.

---

## 5. 컴포넌트 사양

### 5.1 리스트 화면 (전 메뉴 공통 뼈대)
```
┌─ 페이지 헤더: 타이틀(22px SUIT) ─────────── [엑셀 다운로드][주요 액션] ┐
├─ 필터 바: surface-2, 라운드 10px ──────────────────────────────────┤
│   기간 ▾ | 병원 ▾ | 공급사 ▾ | 상태(세그먼트) | 검색 ⌕ | [적용] [초기화] │
│   활성 필터는 하단에 제거 가능한 틸 칩으로 나열                          │
├─ 결과 요약 줄: "총 1,248건 · 합계 ₩34,120,000"(mono) ──── 밀도 토글 ──┤
├─ 테이블 ─────────────────────────────────────────────────────────┤
│   헤더 sticky, 12px 대문자 / 행 높이 44px(컴팩트 36px) / hover surface-2 │
│   숫자·코드 열은 mono 우측 정렬 / 상태는 StatusBadge / 행 클릭 → 상세    │
│   체크박스 선택 시 하단에 플로팅 액션 바("3건 선택 · 일괄 승인") 슬라이드업 │
├─ 페이징: ‹ 1 2 3 … 42 ›  · 페이지 크기 10/30/50/100 ────────────────┤
```
- 열 정렬 클릭 시 화살표 + 헤더 텍스트 `--ink-900` 전환.
- 로딩은 스피너가 아니라 **스켈레톤 행**(shimmer, 실제 열 구조와 동일 폭).

### 5.2 StatusBadge (상태 배지)
`{시맨틱-100 배경 + 시맨틱-600 텍스트 + 6px 점}` 풀 라운드, 12px, 굵기 500.
```
● 승인(ok) ● 전송됨(info) ● 임시저장(hold) ● 반려(crit) ● 배송중(info) ● 마감(hold)
```
상태 문구는 시스템 전체에서 동일 단어를 쓴다(버튼 "승인" → 배지 "승인" → 토스트 "승인되었습니다").

### 5.3 재고 수준 셀 (테이블 안 미니 게이지)
재고 현황 테이블의 핵심 셀. 숫자만 나열하지 않는다.
```
 128 / 안전 100   [██████████░░]   ← 안전재고 대비 바, ok/warn/crit 자동 변색
```
- 현재고(mono 600) / 안전재고(ink-300) + 폭 64px 미니 바.
- 미달 행은 행 좌측에 3px crit 인디케이터 바.

### 5.4 KPI 카드 (대시보드)
- surface-1, 보더, 라운드 10px. 상단: 라벨(12px, ink-500) + 우측 미니 스파크라인(틸, 24px 높이).
- 중앙: 숫자(SUIT 800, 36px, tabular). 하단: 전월 대비 `▲ 4.2%`(ok/crit 자동).
- **경고성 KPI**(승인 대기, 안전재고 미달, D-30 재고 금액)는 좌측 3px 시맨틱 컬러 바 + 클릭 시 해당 필터가 걸린 리스트로 이동.

### 5.5 Chart.js 공통 테마 (`components/charts/theme.ts` 하나로 강제)
- 폰트 Pretendard 12px / 축·그리드 `--line`, x축 세로 그리드 제거.
- 범례: 상단 우측, 점 스타일. 막대 라운드 4px, 최대 두께 28px.
- 라인: 두께 2px, 점 숨김(hover 시만 4px), fill은 틸 8% 단색(그라데이션 금지).
- 툴팁: `--ink-900` 배경, 흰 텍스트, 라운드 8px, 금액 `₩` mono 포맷.
- 애니메이션 400ms `easeOutQuart` 1회만, `prefers-reduced-motion` 시 0.

### 5.6 알림
- 벨 뱃지: crit 존재 시 빨간 뱃지, 아니면 틸.
- 드롭다운 알림: 좌측 시맨틱 점 + 제목 + 상대시간(mono) + 클릭 시 해당 화면 딥링크.
- 토스트: 우하단, surface-1 + 좌측 4px 시맨틱 바, 4초. **파괴적/중요 작업(승인, 마감, 반려)은 토스트가 아닌 확인 모달** — 모달에는 대상 요약(병원명, N건, 합계 금액 mono)을 반드시 표시해 "무엇을 승인하는지" 보이게 한다.

### 5.7 폼 / 입력
- 입력 높이 38px, 라운드 6px, 포커스 시 보더 틸 + 1px 틸 링(글로우 금지).
- 라벨 12px ink-500 상단 배치, 필수는 틸 점(빨간 별표 금지 — 빨강은 오류 전용).
- 오류: 보더 crit + 하단 12px crit 문구("유통기한 형식이 올바르지 않습니다. 예: 2027-03-31").
- 수량/금액 입력은 mono + 우측 정렬 + 천단위 콤마 자동.

### 5.8 빈 화면 / 오류 화면
- 빈 화면: 연한 라인 일러스트(틸 단색 스트로크) + 한 줄 안내 + 주 액션 버튼.
  예: "아직 등록된 사용분이 없습니다" + [사용분 등록].
- 오류: 무엇이 잘못됐고 어떻게 하는지. "저장에 실패했습니다" 금지 →
  "마감된 2026-06월 데이터는 수정할 수 없습니다. 본사 관리자에게 마감 취소를 요청하세요."

---

## 6. 모션 규칙 (절제가 원칙)

| 요소 | 모션 | 시간 |
|---|---|---|
| 페이지 전환 | 콘텐츠 fade + 8px 상승 | 200ms |
| 드롭다운/팝오버 | scale 0.98→1 + fade | 150ms |
| 스캔 성공 | Scan Pulse (4.3) | 300ms |
| 일괄 액션 바 | 하단 슬라이드업 | 200ms |
| 숫자 KPI | 카운트업 (최초 로드 1회만) | 600ms |
| 테이블 행 | hover 배경 전환만 | 120ms |

- 나머지는 전부 무모션. `prefers-reduced-motion: reduce` 시 모든 모션 제거.
- 이징은 `cubic-bezier(0.2, 0, 0, 1)` 하나로 통일.

---

## 7. 품질 기준 (모든 화면 체크리스트)

- [ ] 모바일(390px)에서 테이블은 카드형 리스트로 전환, 스캔/사용분 등록 화면은 모바일 우선
- [ ] 키보드: 테이블 행 포커스 이동, Enter로 상세, 모든 액션 포커스 링 표시(틸 2px)
- [ ] 색 대비 WCAG AA — 특히 warn 텍스트는 흰 배경 위 `--warn-600` 이상만 사용
- [ ] 상태를 색으로만 전달 금지 — 배지는 항상 점+텍스트, 게이지는 항상 숫자 병기
- [ ] 다크 모드에서 시맨틱 색 채도 -10% 보정 토큰 사용
- [ ] 모든 금액 `₩1,234,000` mono, 모든 날짜 `2026-07-22` 고정 포맷
- [ ] 스크린샷 셀프 리뷰: 화면 완성 시 "장식 하나를 뺄 수 있는가?"를 먼저 검토

---

## 8. 구현 지시 (Claude Code용)

1. Phase 2 시작 시 가장 먼저 `resources/css/tokens.css`(위 변수 전체) + `tailwind.config` 매핑 + `resources/js/charts/theme.js`를 만든다.
2. `resources/views/components/`에 `<x-flow-rail>`, `<x-lot-chip>`, `<x-status-badge>`, `<x-stock-level-cell>`, `<x-kpi-card>`, `<x-data-table>`, `<x-filter-bar>`, `<x-scan-input>`을 공통 Blade 컴포넌트로 먼저 완성하고, 이후 모든 화면(Livewire 뷰 포함)은 이것만 조립한다. **화면 안에서 스타일 즉석 작성 금지.** 모션·인터랙션(§6, Scan Pulse 포함)은 Alpine.js 디렉티브와 `resources/js/scan.js`로 구현한다.
3. 색상 하드코딩 금지 — 항상 토큰 변수 참조.
4. 새 화면을 만들 때마다 이 문서의 §5.1 뼈대와 §7 체크리스트를 통과시킨 뒤 다음 화면으로 넘어간다.
